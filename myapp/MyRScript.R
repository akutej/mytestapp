#read.table("example.csv", header=TRUE, sep=";", dec=".")
#testtable <- read.csv(file = 'myapp/Data/data.csv')

testtable <- read.table("myapp/Data/data.csv", header=TRUE, sep=";", dec=".")
head(testtable)
